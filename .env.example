# Client Environment Variables

# API URL for the backend server (when using the real Express backend)
# Leave empty to use the built-in mock API for in-browser demo
VITE_API_URL=http://localhost:5000/api
